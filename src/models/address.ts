export type IAddress = {
  id: string;
  address?: string;
  contact_id: string;
  lat: string;
  lng: string;
  user_id: string;
  user_name: string;
  user_phone: string;
  address_name?: string;
};
